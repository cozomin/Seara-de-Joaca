<div class="galerie-a">
    <div id="grid-animat">
        <% for(let img of imagini) { %>
            <figure>
                <img src="<%= img.fisier %>" alt="<%= img.alt ? img.alt : img.nume %>">
                <figcaption><%= img.nume %></figcaption>
            </figure>
        <% } %>
    </div>
</div>